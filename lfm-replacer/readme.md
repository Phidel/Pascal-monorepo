# lfm-replacer

Замены по списку в текстовых файлах, в частности, замена компонент в формах lazarus

регистронезависимые

в папке обрабатывает все файлы `*.lfm` (lazarus), но можно любые текстовые файлы

Использование: 
```bash
LFMReplacer <путь_к_директории>
```

